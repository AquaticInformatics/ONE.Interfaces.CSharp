name: Build pr

on: 
  pull_request:
    types: [opened, synchronize, reopened, labeled]

jobs:
  call-reusable-build:
    uses: ./.github/workflows/reusable-build.yml
    with:
      publish: true
      prerelease: true
    secrets: inherit
